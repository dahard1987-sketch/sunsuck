{
  "name": "canb-b5-worksheet-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Static B5 worksheet editor with direct PDF export and local PDF optimization",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8000"
  }
}
